<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".activities.sessionActivities.DialogActivity">

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Отправить"
        android:id="@+id/sendButton"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true" />

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/messageEditText"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true"
        android:layout_toStartOf="@+id/sendButton"
        android:layout_alignParentLeft="true"
        android:layout_toLeftOf="@+id/sendButton" />

    <Button
        style="?android:attr/buttonStyleSmall"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Назад"
        android:id="@+id/cancelDialogButton"
        android:height="20dp"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true"
        android:layout_alignParentLeft="true" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceLarge"
        android:id="@+id/userInfoTextView"
        android:layout_alignTop="@+id/cancelDialogButton"
        android:layout_alignEnd="@+id/sendButton"
        android:layout_toEndOf="@+id/cancelDialogButton"
        android:layout_alignBottom="@+id/cancelDialogButton"
        android:text="User info"
        android:gravity="center_vertical"
        android:layout_alignRight="@+id/sendButton"
        android:layout_toRightOf="@+id/cancelDialogButton" />

    <ListView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/messageListView"
        android:layout_below="@+id/cancelDialogButton"
        android:layout_alignParentStart="true"
        android:layout_above="@+id/messageEditText"
        android:layout_alignParentLeft="true" />
</RelativeLayout>
